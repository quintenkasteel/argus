module Test5 where

-- Large function with multiple arguments and complex logic
largeFunc ::
  LocationId ->
  PageId ->
  UserId ->
  PostId ->
  CommentId ->
  ProductId ->
  OrderId ->
  SessionId ->
  CategoryId ->
  TagId ->
  ImageId ->
  VideoId ->
  ArticleId ->
  AuthorId ->
  ProjectId ->
  TaskId ->
  MemberId ->
  FileId ->
  TeamId ->
  GroupId ->
  Text
largeFunc loc page user post comment product order session category tag image video article author project task member file team group = "Test"
